Machine.WorkArea.size(xmin=0,xmax=10,ymin=0,ymax=222);
draw { }

function number boolToNumber(number a,bool b){
a = b;
bool B = true;
/*number n = true; kan ikke parses bør tilføje til error listen frem for at crashe  */

/*number M = 3>2;   kan ikke parses bør tilføje til error listen frem for at crashe  */

number N = B;
N = ABoolianFunc();/* Burde give en typemismatch fejl */
return B;/* Burde give en typemismatch fejl */
}

function bool ABoolianFunc(){
return true;
} 
